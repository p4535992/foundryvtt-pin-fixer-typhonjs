{
  "name": "pin-fixer",
  "title": "Pin Fixer",
  "description": "Make map pins remain the same size regardless of zoom level.",
  "version": "1.3.0",
  "scripts": {
    "build": "vite build",
    "dev": "vite",
    "eslint": "eslint .",
    "package": "gulp package",
    "buildold": "gulp clean && gulp build && gulp link",
    "build:watch": "gulp watch",
    "clean": "gulp clean && gulp link --clean",
    "prettier-format": "prettier --config .prettierrc --write \"./src/**/*.(ts|js|json|scss|css)\""
  },
  "dependencies": {
    "@typhonjs-fvtt/runtime": "^0.0.19",
    "@typhonjs-fvtt/svelte-standard": "^0.0.8",
		"svelte": "^3.49.0",
		"svelte-preprocess": "^4.10.7"
  },
  "devDependencies": {
    "@typhonjs-config/eslint-config": "^0.3.0",
    "@typhonjs-fvtt/eslint-config-foundry.js": "^0.8.0",
		"eslint": "^8.22.0",
		"eslint-config-arcanist": "^1.5.0",
    "svelte-preprocess": "^4.10.0",
    "vite": "^3"
  },
  "browserslist": [">5%", "not IE 11"]
}
